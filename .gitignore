# Model weights (downloaded by scripts/download_weights.py)
weights/

# C++ / CUDA build output (out-of-source trees: build/, build-cuda/, ...)
build*/
*.o
*.obj
*.exe
__cmake_systeminformation

# Paper corpus and processed data
data/papers/
data/chunks/

# Environment / secrets
.env
.venv/

# Python
__pycache__/
*.pyc

# Note: reference/ (golden test oracles) is intentionally NOT ignored -
# token_ids.npy / logits.npy are small and serve as engine/tests/ fixtures.

# remove jsx
atlas-repo-structure.jsx
